# Copy to .env.local (not committed) — Next.js only loads env from the frontend/ folder.
# Match Project/.env. Restart `npm run dev` after any change.
#
# Direct Django:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Nginx :8080 (same origin as the site, /api -> Django)
# NEXT_PUBLIC_API_URL=http://localhost:8080
