apiVersion: v1
kind: Service
metadata:
  name: acu-frontend
  namespace: acu-smart-assistant
spec:
  selector:
    app: acu-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000

