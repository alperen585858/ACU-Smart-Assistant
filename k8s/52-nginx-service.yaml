apiVersion: v1
kind: Service
metadata:
  name: acu-nginx
  namespace: acu-smart-assistant
spec:
  type: NodePort
  selector:
    app: acu-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080

